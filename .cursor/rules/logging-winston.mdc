---
description: Logging via Winston, no console.log
globs: apps/api/**/*.ts, apps/worker-import/**/*.ts, apps/worker-publish/**/*.ts, apps/worker-sync-stock/**/*.ts, packages/worker-lib/**/*.ts, packages/shared/**/*.ts
alwaysApply: false
---

# Logging

## Winston Only

- Use `createLogger(label)` from `@seller/shared` for all logging
- **No** `console.log`, `console.error`, `console.warn`, `console.debug` — banned in backend code

## Usage

```typescript
// ✅ GOOD
import { createLogger } from '@seller/shared';

const log = createLogger('MyService');
log.i('Message');
log.e('Failed', error);
log.w('Deprecated', { id });
log.d('Debug data', obj);
log.http('GET /api/foo 200 12ms', { method: 'GET', statusCode: 200 });
```

API: `log.i()` info, `log.d()` debug, `log.w()` warn, `log.e()` error, `log.http(msg, meta)` access-логи.

```typescript
// ❌ BAD
console.log('message');
console.error('error', err);
```

## NestJS API

- Inject `LoggerService` in services/controllers for Nest-aware context
- Nest bootstrap uses it automatically; internal Nest logs go through Winston too
