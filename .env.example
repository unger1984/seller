DATABASE_URL="postgresql://seller:<PASSWORD>@localhost:5432/seller"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me"
CREDENTIALS_ENCRYPTION_KEY="32-bytes-hex-or-any-secret-string-here"
FRONTEND_URL="https://localhost:8443"

# CORS: пусто = отключено; через запятую — origins для allow
# CORS_ORIGINS="https://localhost:8443"

# SMTP: пусто SMTP_HOST = jsonTransport (логирует в консоль)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM=noreply@seller.local
# SMTP_DRY_RUN=false
