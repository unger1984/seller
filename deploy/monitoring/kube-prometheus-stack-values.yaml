# kube-prometheus-stack для stage
# Helm: prometheus-community/kube-prometheus-stack
# Версию чарта фиксировать: --version X.Y.Z

prometheus:
  prometheusSpec:
    retention: 7d
    retentionSize: 5GB

grafana:
  enabled: true
  service:
    type: NodePort
    nodePort: 30300
